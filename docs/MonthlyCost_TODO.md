# MonthlyCost Summary - TODO List

เอกสารนี้ใช้สำหรับจดบันทึกรายการฟีเจอร์หรือการปรับปรุงที่ยังคงค้างอยู่ (Pending Tasks) สำหรับหน้าจอ `http://localhost:5125/MonthlyCost/Summary`

## 📋 รายการสิ่งที่ต้องทำ (Pending)

- [ ] **ฟีเจอร์ Export Excel:** สร้างระบบสำหรับ Export ข้อมูลตารางสรุปรายปี/รายเดือน ออกมาเป็นไฟล์ `.xlsx` (ตามโครงสร้างข้อมูลตารางปัจจุบัน)

## ✅ รายการที่ทำเสร็จแล้ว (Completed)

- [x] จัดกลุ่มข้อมูลตามเดือน (Group by Month) 
- [x] เรียงลำดับกลุ่มเดือนจากล่าสุดไปเก่าสุด (12 -> 1)
- [x] แยกกลุ่ม "ยังไม่กำหนดเดือนรับ/ชำระ" ไว้ด้านล่างสุด
- [x] ระบบซ่อน/แสดงกลุ่มเดือน (Collapse/Expand)
- [x] ย่อชื่อรายการ (OrderName) ด้วย `...` และแสดง Tooltip เมื่อนำเมาส์ไปชี้
- [x] ค้นหาและกรองข้อมูลแบบ Real-time พร้อมแสดงสถิติ KPI ประจำเดือน
- [x] จัดเรียงข้อมูล (Sort) ภายในกลุ่มเดือน
- [x] แสดงข้อความแจ้งเตือน "ยังไม่มีรายการ" สำหรับเดือนปัจจุบันที่ไม่มีข้อมูล
- [x] ปรับ UI/UX ให้ดูสะอาดและใช้งานง่ายขึ้น

---
*หากมีฟีเจอร์อื่นที่ต้องการเพิ่ม สามารถแจ้งให้ระบบจดบันทึกเพิ่มลงในไฟล์นี้ได้เลยครับ*
